-- This command inserts a new row into the 'first_table' table
-- The row will have 'id' = 89 and 'name' = 'Best School'
-- Each execution of this script will add a new identical row
-- Make sure text values are enclosed in single quotes and numeric values are not
INSERT INTO first_table (id, name)
VALUES (89, 'Best School');